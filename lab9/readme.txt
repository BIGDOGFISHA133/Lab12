getmostfreqchar.c takes an input test.txt and returns its most frequent letter in the txt file

addressofscalar.c returns the memory address of random variables char "charvar" and int "intvar"

addressofarray.c returns the address of 5 integers in an array, as well as the size and the length of the array